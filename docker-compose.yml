version: '3'

services:
  organization_app:
    build: .
    restart: unless-stopped
    command:
      - python organization_main.py
    volumes:
      - images:/bot/photos/
      - ./:/bot/

volumes:
  images: